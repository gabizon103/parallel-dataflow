mod mutex_worklist;
use lesson4::framework::{AnalysisFramework, ReachingDefsGeneric};
use std::collections::{HashMap, HashSet};

fn main() {
    // let reaching_defs_framework: AnalysisFramework<HashMap<String, HashSet<(usize, usize)>>> =
    //     AnalysisFramework::new(cfg, init);

    println!("Hello, world!");
}
